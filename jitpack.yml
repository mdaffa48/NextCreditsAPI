# jitpack.yml
jdk:
  - openjdk21

before_install:
  - sdk install java 21.0.2-open
  - sdk use java 21.0.2-open
  - sdk install maven
  - mvn -v

install:
  - mvn install -Dmaven.javadoc.skip=true -DskipTests

maven:
  version: 3.9.8